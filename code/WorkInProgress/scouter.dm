
/obj/item/clothing/glasses/hud/scouter
	name = "scouter"
	desc = "Used to check if anything is over 9000."
	icon_state = "scouter"
	item_state = "scouter"

/*
	proc
		checkpowerlevel(var/mob/M)
		if(!M)	return
		if(!M.client)	return
		var/client/C = M.client
		var/icon/tempHud = 'hud.dmi'
		for(var/mob/living/carbon/human/player in view(M))
			//THIS line is where the code starts. Will be made once SQL server/database is set up
*/
